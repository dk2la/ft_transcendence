<%= form_with(model: chat_room, local: false) do |form| %>
    <% if chat_room.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(chat_room.errors.count, "error") %> prohibited this chat_room from being saved:</h2>
        <ul>
          <% chat_room.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div style="color: white; font-family: fantasy;">
      <%= form.label "Add a new room" %><br />
      <%= form.text_field :name, placeholder: "Room name", class: "text_sign_up" %>
    </div></br>
  
    <%= form.submit "Create", class: "button_start_game" %></br></br>
    <%= link_to 'Back', chat_rooms_path, class: "button_make_officer" %>
  <% end %>